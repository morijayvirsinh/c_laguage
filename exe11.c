
#include<stdio.h>
int main()
{
    int num;
    printf("enter the number");
    scanf("%d",&num);
    if(num>0 && num<10)
    {
        printf(" number is greater than 0 and less than 10");
    }
    return 0;
}